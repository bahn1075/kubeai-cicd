apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: litellm
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/BerriAI/litellm.git
      targetRevision: main
      path: deploy/charts/litellm-helm
      helm:
        parameters:
          - name: image.repository
            value: ghcr.io/berriai/litellm-database
          - name: image.tag
            value: main-latest
          - name: postgresql.image.tag
            value: latest
        valueFiles:
          - $values/litellm/values.yaml
    - repoURL: https://github.com/bahn1075/kubeai-cicd.git
      targetRevision: a10g
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: litellm
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
